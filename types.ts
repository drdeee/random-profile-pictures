export interface Config {
    apiKey: string;
    apiSecret: string;
    apiToken: string;
    clientId: string;
    clientSecret: string;
}

export interface Auth {
    accessToken: string;
    accessSecret: string;
}